<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>dev.gaphunter.mermaidcompanion</id>
    <name>Mermaid Companion</name>
    <vendor>Gap Hunter Labs</vendor>
    <description><![CDATA[
        <p>Syntax highlighting and real validation for .mmd/.mermaid
        diagram source files (flowchart, sequence, and class diagrams): a
        real paid competitor's reviewers reported it "shows false warnings
        for valid non-ASCII subgraph identifiers" and "false errors for
        valid node shapes where the node contains non-ASCII characters" --
        this plugin's lexer is Unicode-aware throughout (no ASCII-only
        character-class check anywhere in the identifier/label path), so
        those exact false positives structurally cannot happen here.</p>
        <p>Unterminated string literals, unmatched/mismatched node-shape
        brackets, and `subgraph` blocks missing their `end` are flagged as
        real errors -- exactly the "does it actually validate my syntax" gap
        the cited 1-star review described.</p>
        <p>A "Preview" tab renders the diagram visually via the real
        mermaid.js (bundled), next to the text editor -- the direct fix for
        that same review ("It opens and shows me a bunch of icons on page.
        That's it. No other functionality at all."). If this IDE build
        doesn't support embedded browser rendering, the tab shows a plain
        message instead; everything else in the plugin is unaffected.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">
        <!-- language/filetype values renamed from "Mermaid" to
             "MermaidCompanion": JetBrains bundles native Mermaid support
             (com.intellij.mermaid) as of IU-262.9437.65 using the same
             "Mermaid" Language ID, which crashed plugin load via
             ImplementationConflictException the moment both are
             installed together (confirmed by JetBrains's own External
             Plugins Checker Tests, TeamCity build #369805). -->
        <fileType name="MermaidCompanion" implementationClass="dev.gaphunter.mermaidcompanion.lang.MermaidFileType"
                   fieldName="INSTANCE" language="MermaidCompanion" extensions="mmd;mermaid" order="first"/>
        <fileTypeDetector implementation="dev.gaphunter.mermaidcompanion.detection.MermaidFileTypeOverrider"/>
        <lang.parserDefinition language="MermaidCompanion"
                                implementationClass="dev.gaphunter.mermaidcompanion.lang.MermaidParserDefinition"/>
        <lang.syntaxHighlighterFactory language="MermaidCompanion"
                                        implementationClass="dev.gaphunter.mermaidcompanion.lang.MermaidSyntaxHighlighterFactory"/>
        <editorHighlighterProvider filetype="MermaidCompanion"
                                    implementationClass="dev.gaphunter.mermaidcompanion.lang.MermaidEditorHighlighterProvider"/>
        <annotator language="MermaidCompanion" implementationClass="dev.gaphunter.mermaidcompanion.highlighting.MermaidKeywordAnnotator"/>
        <annotator language="MermaidCompanion" implementationClass="dev.gaphunter.mermaidcompanion.highlighting.MermaidSyntaxAnnotator"/>
        <fileEditorProvider implementation="dev.gaphunter.mermaidcompanion.preview.MermaidPreviewFileEditorProvider"/>
    </extensions>
</idea-plugin>
